-- Jeu de données de démo — à exécuter uniquement en local/dev.
-- Remarque : les comptes auth.users doivent être créés via Supabase Auth
-- (dashboard, CLI `supabase` ou l'app elle-même) avant de pouvoir
-- promouvoir un profil en admin ci-dessous.

-- Exemple : promouvoir un utilisateur existant en admin
-- update public.profiles set role = 'admin' where email = 'admin@example.com';
